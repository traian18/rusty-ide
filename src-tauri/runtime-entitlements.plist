<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Claude (Bun) and Copilot (V8) are JIT-compiled JS engines. Under
	     hardened runtime, without these two, they can't reserve executable
	     memory: Claude fails with "SharedArrayBuffer is not defined", Copilot
	     fails with "Failed to reserve virtual memory for CodeRange". -->
	<key>com.apple.security.cs.allow-jit</key>
	<true/>
	<key>com.apple.security.cs.allow-unsigned-executable-memory</key>
	<true/>
	<key>com.apple.security.cs.disable-library-validation</key>
	<true/>
</dict>
</plist>
