{
  "name": "rusty-vendor-cli",
  "private": true,
  "version": "1.0.0",
  "description": "Version lock source for on-demand managed runtimes. Native package URLs and checksums are embedded in src-tauri/src/harness/managed-runtime-packages.json; dependencies are not installed or bundled during app builds.",
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.216",
    "@github/copilot-sdk": "1.0.7",
    "@openai/codex": "0.144.6"
  }
}
